apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sumo-k8

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - storageclass.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # - postgres.yaml    # Uncomment for in-cluster PostgreSQL
  # - ingress.yaml     # Uncomment for Ingress
  # - karpenter-nodepool.yaml     # For EKS with Karpenter
  # - karpenter-nodeclass.yaml    # For EKS with Karpenter

commonLabels:
  app: sumo-k8-controller
  managed-by: kustomize

