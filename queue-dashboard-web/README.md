# Queue Dashboard Web

Next.js dashboard that polls the Queue Dashboard API and renders:
- queue counts by status
- submission volume over time
- a recent jobs table

## Environment
- `DASHBOARD_API_URL` (server-to-server, from Next API routes). Default: `http://queue-dashboard-api:8000`.

